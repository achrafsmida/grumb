parameters:
    git_dir: "./"
    bin_dir: vendor/bin
    tasks:

        phpcs:
            standard: PSR2
            tab_width: ~
            sniffs: []
            triggered_by: [php]
        phpcsfixer2:
            allow_risky: false
            cache_file: ~
            config: ~
            rules: []
            using_cache: true
            verbose: true
        phpunit: ~
