parameters:
    git_dir: "."
    bin_dir: vendor/bin
    tasks:

        phpcs:
            standard: ~
            tab_width: ~
            sniffs: []
            triggered_by: [php]
            ignore_patterns:
                    - "app"
                    - "var"
                    - "web"

        phpcsfixer: ~
